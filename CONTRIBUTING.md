# Contributing

## Development Loop
1. Add/modify feature modules behind typed contracts.
2. Run `npm run lint && npm test`.
3. Keep framework-layer changes backwards compatible.

## Commit Style
Prefer small commits with clear intent prefixes.
